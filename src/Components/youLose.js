import React from "react";

const Lose = props => {
  return (
    <p className="lose">
      Bad luck! You ran out of guesses. Use the buttons below to play again with
      the same word or try a new word.
    </p>
  );
};

export default Lose;
